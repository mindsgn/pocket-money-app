import { View, TextInput } from 'react-native';

export const Input = () => {
  return (
    <View
      style={{
        width: '90%',
        borderWidth: '4',
        borderColor: 'white',
      }}
    >
      <TextInput />
    </View>
  );
};
