import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  default: {
    padding: 10,
    display: 'flex',
    width: '100%',
    height: '100%',
    backgroundColor: 'black',
    paddingBottom: 100,
  },
});
