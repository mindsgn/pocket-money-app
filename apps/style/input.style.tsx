import { StyleSheet } from 'react-native';

export const input = StyleSheet.create({
    default: {
        width: '100%',
        color: 'white',
        borderColor: 'white',
        borderWidth: 4,
        borderRadius: 20,
        padding: 10,
        paddingLeft: 20,
        marginTop: 20,
        marginBottom: 20
    }
});
