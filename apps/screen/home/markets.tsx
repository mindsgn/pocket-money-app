import React from 'react';
import { View, Text, } from 'react-native';
import { container } from '../../style/container';
import Card from '../../components/card';

const Markets = ({ navigation } : { navigation: any }) => {
    return (
      <View>
      </View>
  );
};

export default Markets;
